#include <stdio.h>
#include <stdlib.h>

int mainHelp(void) {

  /*
    \n = quebrar a linha;
    \t = tabulação;
    \\ = para imprimir uma barra invertida;
    \" = para imprimir aspa duplas;
    \' = para imprimir aspa simples;
    \0 = para finalizar a cadeia de caracteres;
  */
  return 0;
}
